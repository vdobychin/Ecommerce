@model ShopCart

<div class="row">
    <div class="col-9">
        @{
            foreach (var product in ViewBag.products)
            {
                <partial name="_ProductCard.cshtml" model="product" />
            }
        }
    </div>
    <div class="col-3">
        <p>Здесь фильтр</p>
    </div>

</div>
